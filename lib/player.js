(function(){	var Player = function(name, url) {		this.score = 0;		this.level = 0;		this.name = name;		this.url = url;	};		Player.prototype.addScore = function(score) {		this.score += score;				if (this.score < 0) {			this.score = 0;		}	}		Player.prototype.nexLevel = function() {		this.level++;	}		exports.Player = Player;})();