(function(){	var Questions = require('./questions');		exports.questionManager = {		getNext: function(level)  {			if (level < this.totalQuestions()){				return new Questions.StoredQuestion(level);			}						var index = Math.floor(Math.random() * (this.totalQuestions()));			return new Questions.StoredQuestion(index);		},				totalQuestions: function() {			return require('./data.js').QuestionRepo.length;		}	};})();