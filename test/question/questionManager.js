var Assert = require('assert');var QuestionManager = require('../../lib/question/questionManager').questionManager;Assert.ok(QuestionManager.totalQuestions() > 0);Assert.ok(QuestionManager.getNext(0));Assert.ok(QuestionManager.getNext(0).getText() != '');var firstQuestion = QuestionManager.getNext(0).getText();var secondQuestion = QuestionManager.getNext(1).getText();Assert.ok(firstQuestion != secondQuestion, 'La primer y segunda pregunta no deben ser iguales');var thirdQuestion = QuestionManager.getNext(2).getText();Assert.ok(thirdQuestion == QuestionManager.getNext(2).getText(), 'La tercer pregunta deben ser siempre la misma');Assert.ok(QuestionManager.getNext(QuestionManager.totalQuestions() + 1).getText(), 'Se debe poder pedir más de la cantidad máxima de preguntas');var aQuestion = QuestionManager.getNext(QuestionManager.totalQuestions() + 1).getText();var anotherQuestion = QuestionManager.getNext(QuestionManager.totalQuestions() + 1).getText();Assert.ok(aQuestion != anotherQuestion, 'Después de la cantidad máxima se devuelven preguntas al azar')